# 起步

## 命令行

- 使用Cobra实现命令行功能（参数输入）
- 调用 init 子命令时，会尝试自动初始化并创建配置文件。

## 配置功能

- 使用Viper实现配置，使用toml格式，使用命令行参数指定配置文件路径，否则则使用默认配置。
- 运行时如果不传递配置文件，则默认在当前路径和 config 子目录中寻找 config.toml 文件。
- 如果使用了 -c 的 flag，则使用指定的路径文件。

## 日志

使用Go内置的Slog实现日志功能，目前仅在`internal/config/config.go`中预留额外功能位置。